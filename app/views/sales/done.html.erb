<h1>El registro fue almacenado con éxito!</h1>

<p id="notice"><%= notice %></p>

<h1>Registro</h1>

<table class="table">
  <thead>
    <tr>
      <th>Codigo</th>
      <th>Detalle</th>
      <th>Categoria</th>
      <th>Valor</th>
      <th>Descuento</th>
      <th>Impuesto</th>
      <th> Valor Total </th>
      <th colspan="7"></th>
    </tr>
  </thead>

  <tbody>
    <% @sales.each do |sale| %>
      <tr>
        <td><%= sale.cod %></td>
        <td><%= sale.detail %></td>
        <td><%= sale.category %></td>
        <td><%= sale.value %></td>
        <td><%= sale.discount %></td>
        <td><%= sale.tax %></td>
        <td><%= sale.total %></td>
     </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Volver', new_sale_path %>
